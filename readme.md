# Personal website static assets/builder.

First, if necessary, clean up any odd residual files (.css, .map, etc.) by running ``cleaner.py``

Then run ``website_builder.py`` in ``/source/_scripts`` to build the website in ``/build/``